# Agent Rules

- presentar plan si se tocan mas de 3 archivos
- resumir cambios al finalizar
- leer `AGENTS.md`, `CLAUDE.md`, `.cursorrules` o `.windsurfrules` antes de actuar cuando exista un editor agéntico.
- validar con `devsteps validate` antes de cerrar cambios de onboarding o publicación.
