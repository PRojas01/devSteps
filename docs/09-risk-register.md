# Risk Register

- la documentación puede desalinearse si cambian los comandos de agentes externos.
- los flujos de DevControl requieren `git init` y `sp-devcontrol init` antes de `project:check`.
- la publicación depende de mantener `devsteps.yaml`, README y docs públicas sincronizadas.
